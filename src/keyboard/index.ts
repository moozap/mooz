/**
 * Functions to calculate and draw fretboard layout
 * @module Keyboard
 */
import type { Player } from '../player'
import type { ScheduleEvent } from '../schedule'

export class KeyboardPlayer implements Player {
  play(event: ScheduleEvent) {}
}
